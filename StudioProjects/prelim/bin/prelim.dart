void main(){
  Function vowels (ch){
    ch = ch.toUpperCase();
    ch == "A" || ch == "E" || ch == "I" || ch == "O" || ch == "U";
  }

  Function numofVowels(str)
  {

    var count = 0;
    for ( var i = 0; i < str.length; i++)
    {
      if (numofVowels(str[i]));

      ++count;
      return count;
    }
}